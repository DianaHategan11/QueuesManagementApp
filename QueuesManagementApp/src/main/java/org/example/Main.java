package org.example;

import org.example.gui.SimulationFrame;
import org.example.logic.StartSimulation;

public class Main {
    public static void main(String[] args) {
        StartSimulation startSimulation = new StartSimulation(new SimulationFrame());
    }
}
